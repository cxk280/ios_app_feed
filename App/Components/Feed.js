import React, { Component } from 'react';
import {
  Text,
  ListView
} from 'react-native';

export default class Feed extends Component {
  render() {
    return (
      // <ListView>
        <Text>This is text in Feed.</Text>
      // </ListView>
    );
  }
}
